/*
    globalTestSetup.ts
    Run by Vitest at start of all testing
    Not currently used.
*/

export async function setup() {
    // Do nothing.
}

// Optionally a teardown function can also be included, to run at the very end:
// export function teardown() {}
